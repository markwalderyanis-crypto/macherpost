# Bild-Server Dependencies — SDXL via diffusers.
# Installation in 2 Schritten (PyTorch zuerst, weil CUDA-Wheel-Index):
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-image.txt
#
flask>=3.0
diffusers>=0.27
transformers>=4.40
accelerate>=0.30
safetensors>=0.4
pillow>=10.0
